FROM postgres:16

RUN apt-get update || apt-get update && \
    apt-get install -y postgresql-16-pgvector && \
    rm -rf /var/lib/apt/lists/*

ENV POSTGRES_USER=chronos_user
ENV POSTGRES_PASSWORD=chronos_pass
ENV POSTGRES_DB=chronos_db

EXPOSE 5432
